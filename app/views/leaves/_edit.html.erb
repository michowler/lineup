<%=form_for @leafe,class: "form-horizontal",remote: true do |form|%>
	
	<%=render partial: "form", :object => form%>

	<%=form.hidden_field :status, :value => "Pending" %>

	<%=form.submit "Submit", class: "btn btn-default"%>
<%end%>